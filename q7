#include<iostream>
using namespace std;
int main()
{
	int arr[]={1,2,3,5,4,6,7,9,8};
	int n=sizeof(arr)/sizeof(arr[0]);
	int cnt=0;
	int i,j;
	
	for(i=0;i<n;i++)
	{
		for(j=i+1;j<n;j++)
		{
			if(arr[i]>arr[j])
			cnt++;
		}
	}
	cout<<"total no of inversions are:"<<cnt;
	return 0;
}
