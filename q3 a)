#include<iostream>
using namespace std;
int main()
{
	int arr[]={1,2,3,5,6,7,8,9};
	int n=sizeof(arr)/sizeof(arr[0]);
	int xor1,xor2;
	
	int i;
	for(i=0;i<n;i++)
	{
		xor2=xor2^arr[i];
		xor1=xor1^(i+1);
	}
	xor1=xor1^n;
	cout<<"missing no is:";
	cout<<(xor1^xor2);
	return 0;
}
